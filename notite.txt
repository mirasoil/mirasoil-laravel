Pasi de urmat:
1.Product si Shop sa se conecteze la aceeasi baza de date

2.Newsletter - /newsletter colecteaza datele dar nu exista - try ajax validation

3.Guests should have access to individual products pages

4.Stilizare butoane si card-uri pagina principala

5.Search bar with  ajax - the search page is stable, I need it integrated in nav section

6.Meniu pentru desktop -- ?


7.Left-sidebar - configurare rute
8.In shop.blade.php trebuie sa pun un link spre pagina cu fiecare produs individual - guests can't access individual pages


9.Animatie - meh..


10.Cos (https://github.com/Crinsane/LaravelShoppingcart)
11.Cashier/Stripe


!!!Pe viitor, ShopController si ProductsController se vor conecta la acelasi model - momentan doar adminul are acces la pagina individuala a fiecarui produs


12.In tabela useri mai am nevoie de: adresa, nr de telefon, eventual nume prenume separat




I need a table for cart...
NOT WORKING
// add cart items to a specific user
    // $userId = auth()->user()->id; // or any string represents user identifier
    // Cart::session($userId)->add(array(
    //     'name' => $cart->name,
    //     'price' => $cart->price,
    //     'quantity' => $cart->quantity,
    //     'associatedModel' => $ProductController
    // ));
