De rezolvat: redirectionare pentru utilizatori neautentificati pe pagina de login/user nu login !


Pasi de urmat:
1.Product si Shop sa se conecteze la aceeasi baza de date - done - second check on routes 
-afisarea produselor se face acum doar din ProductController cu functii separate pentru fiecare tip de utilizator
-in ShopController se pastreaza doar crud-ul pentru cos (fara vizualizare)

!!!ATENTIE:-user nelogat - accesare cos -este redirectionat spre pagina de login (route = login) care nu exista teoretic, trebuie modificat, by default ruta de login sa fie login/user
-solved by adding separate route name for '/login', using the same controller with showUserLoginForm function

2.Newsletter - /newsletter colecteaza datele dar nu exista - try ajax validation
-se primeste mail de confirmare-mailchimp => stocare in baza de date


4.Stilizare butoane si card-uri pagina principala

5.Search bar with  ajax - autocomplete + styling

6.Meniu pentru desktop -- ?


7.Left-sidebar - configurare rute
8.In shop.blade.php trebuie sa pun un link spre pagina cu fiecare produs individual 
-cart: link spre pagina individuala produs

9.Animatie - meh..


10.Cos (https://github.com/Crinsane/LaravelShoppingcart)


!!!Pe viitor, ShopController si ProductsController se vor conecta la acelasi model 
La implementare - folosind acelasi controller, adminii au acces la /details/{id} si shop (resource for products I guess) - solved by adding if statement for admin guard in ProductController
-pe viitor - o singura functie atat pentru index cat si pentru show implementata cu un switch-case sau chiar si cu if !!


I need a table for cart...
NOT WORKING
// add cart items to a specific user
    // $userId = auth()->user()->id; // or any string represents user identifier
    // Cart::session($userId)->add(array(
    //     'name' => $cart->name,
    //     'price' => $cart->price,
    //     'quantity' => $cart->quantity,
    //     'associatedModel' => $ProductController
    // ));




Obs. pretul trebuie modificat - decimal

In revieworder - la actualizare date, daca nu se selecteaza judetul acesta se pune by default null la actualizare



Partea de GraphQL - can't find the Type, there are no relationships between tables yet so it won't work - am renuntat pe moment - info guzzle